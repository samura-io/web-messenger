.Chat {
    margin-top: 20px;
    border-top: 1px solid var(--divider);
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    overflow-y: auto;

    @mixin transition-all;
}
