.NavigationTo {
    color: var(--textGray);
    text-decoration: none; 
    margin-bottom: 23px;
    @mixin font 500, 12px, 12px;
    @mixin opacity-transition;

    &_position_left {
        text-align: right;
    }

    &:hover {
        @mixin opacity-hover;
    }
}